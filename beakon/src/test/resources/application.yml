server:
  port: 7001

spring:
  profiles:
    active:
    - test
  application:
    name: BEAKON-SERVICE
  cloud:
    discovery:
        enabled: false
    config:
      enabled: false

avro:
  topic:
    standings: "apiscraper.standings.avro.topic.v1"
