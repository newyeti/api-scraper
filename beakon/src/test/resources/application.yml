server:
  port: 4000

spring:
  application:
    name: BEAKON-SERVICE
  config:
    import: "optional:configserver:http://config-server:9091"

avro:
  topic:
    standings: "apiscraper.standings.avro.topic.v1"